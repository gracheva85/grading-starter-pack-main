export type TypeMenu = {
  QUESTS: {
      name: string;
      route: string;
  };
  BEFINNERS: {
      name: string;
      route: string;
  };
  REVIEWS: {
      name: string;
      route: string;
  };
  STOCKS: {
      name: string;
      route: string;
  };
  CONTACTS: {
      name: string;
      route: string;
  };
}
